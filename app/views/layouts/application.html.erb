<!DOCTYPE html>
<html>
<head>
  <title>Tasks</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<div id="header">
    <% if user_signed_in? %>
        <div style="float: right;">
            Hello <%= current_user.name || current_user.email %>
            <%=link_to "Sign Out", "/users/sign_out"%>
        </div>
    <%else%>
        <div style="float: right;"><%= link_to "Sign In", "/users/sign_in"%></div>
    <%end%>
</div>
<%= yield %>

</body>
</html>
